<?xml version="1.0" encoding="utf-8"?>
<!-- Base shape only - the actual status color is applied at runtime via
     backgroundTintList (see StatusColors.kt), since it depends on data. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/status_stable" />
</shape>
