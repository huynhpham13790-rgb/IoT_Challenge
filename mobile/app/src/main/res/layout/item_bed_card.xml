<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bedCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginTop="10dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/border"
    app:strokeWidth="1dp"
    app:rippleColor="@color/accent_soft">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Same idea as the web dashboard's colored left stripe: the status
             is readable at a glance without reading any text at all. -->
        <View
            android:id="@+id/statusStripe"
            android:layout_width="5dp"
            android:layout_height="match_parent"
            android:background="@color/status_stable" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/bedIdText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textSize="16sp"
                        android:textStyle="bold"
                        android:textColor="@color/text_primary"
                        tools:text="BED-01" />

                    <TextView
                        android:id="@+id/roomText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textSize="12sp"
                        android:textColor="@color/text_dim"
                        tools:text="ICU-1" />
                </LinearLayout>

                <include android:id="@+id/statusChip" layout="@layout/item_status_chip" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="horizontal">

                <include android:id="@+id/vitalSpo2" layout="@layout/item_vital_inline" />
                <include android:id="@+id/vitalHr" layout="@layout/item_vital_inline" />
                <include android:id="@+id/vitalDrip" layout="@layout/item_vital_inline" />
            </LinearLayout>

            <TextView
                android:id="@+id/alertMessageText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textSize="12sp"
                android:textColor="@color/status_critical"
                android:visibility="gone"
                tools:visibility="visible"
                tools:text="Tắc dây truyền — giọt chậm lại và túi dịch không nhẹ đi" />

            <TextView
                android:id="@+id/updatedText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:textSize="10sp"
                android:textColor="@color/text_dim"
                tools:text="Cập nhật 2 giây trước" />

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
